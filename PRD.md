# PRD: 독서/영화 기록 트래커

## 1. 개요
책과 영화를 다 보고 난 뒤 제목, 평점, 한줄평, 완료일을 기록하고
갤러리(카드형) 화면에서 한눈에 조회할 수 있는 개인용 웹앱.
별도 DB 없이 Notion 또는 Google Sheets를 데이터 저장소로 사용한다.

## 2. 문제정의
- 책/영화를 감상한 직후에는 느낌이 생생하지만 기록을 안 남기면 금방 잊어버린다.
- 이미 쓰고 있는 Notion이나 Sheets에 기록을 남기고 싶지만, 보기 좋은 갤러리 형태로
  볼 수 있는 전용 화면이 없다.
- 별도 앱 설치나 회원가입 없이, 이미 쓰는 도구를 그대로 백엔드로 활용하고 싶다.

## 3. 목표 (Goals / Non-goals)
**Goals**
- Notion과 Google Sheets 중 선택해서 CRUD가 가능한 갤러리 웹앱 제공
- 커버이미지(URL)를 포함한 카드형 UI로 표시
- GitHub로 버전 관리, Vercel로 자동 배포

**Non-goals**
- 다중 사용자 로그인/공유 기능 (개인용으로 한정)
- 커버이미지 자동 검색(외부 API 연동)은 1차 범위 아님 — URL 직접 입력만 지원
- 모바일 네이티브 앱

## 4. 핵심기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| 갤러리 조회 (Read) | Notion/Sheets 데이터 중 **상태=완료**인 항목만 완료일 최신순으로 표시 (읽는 중/보류 상태는 갤러리에 노출하지 않음) | P0 |
| 새 기록 추가 (Create) | 입력 폼으로 항목 추가. **상태 값은 폼에 노출하지 않고 항상 "완료"로 자동 저장** | P0 |
| 평점/한줄평 수정 (Update) | 카드 클릭 시 수정 | P0 |
| 삭제 (Delete) | Notion=보관 처리, Sheets=소프트 삭제 | P1 |
| 데이터 소스 토글 | Notion ↔ Google Sheets 전환 | P0 |
| 필터/정렬 | 유형별 필터(책/영화), 완료일순·평점순 정렬 | P1 |
| 커버이미지 표시 | URL 기반 이미지, 실패 시 placeholder | P1 |
| GitHub 버전 관리 | 기능 단위 커밋/푸시 | P1 |
| Vercel 배포 | GitHub 연동 자동 배포 | P1 |

## 5. 사용자 시나리오
- 책을 다 읽은 사용자가 "+ 새 기록 추가"를 눌러 제목/평점/한줄평/완료일/커버이미지
  URL을 입력하면, 갤러리에 새 카드가 즉시 나타난다.
- 지난 기록 중 평점을 잘못 매긴 걸 발견한 사용자가 카드를 클릭해 평점을 수정한다.
- 사용자가 "영화"만 필터링해서 평점 높은 순으로 정렬해 다시 보고 싶은 영화를 고른다.

## 6. 기술요구사항
- Frontend: React + Vite
- 스타일: Tailwind CSS, 아이콘: lucide-react
- **개발 순서**: 화면(가짜 데이터) → 실제 데이터 연결 → 예외 처리 순서로 진행
- **개발 중**: 별도 서버 없이 연결된 Notion, Google Sheets 도구를 그대로 사용해 CRUD 구현·테스트
- **배포 시**: 개발 중 쓰던 연결 방식은 배포된 웹사이트에서는 그대로 쓸 수 없으므로,
  배포 단계에서 안전한 방식(서버 쪽 코드 + 환경변수)으로 전환한다. 인증 정보는
  절대 화면(프론트엔드) 코드에 노출하지 않는다
- 버전 관리: GitHub과 연동해 기능 단위로 커밋/푸시
- 배포: Vercel, GitHub 저장소와 연동해 push 시 자동 배포
- 상태 관리: React useState/useContext (별도 라이브러리 불필요)
- 에러 처리: 데이터 호출 실패 시 사용자 알림, 로딩 중 스켈레톤 UI, 데이터가 0건이어도 화면이 깨지지 않을 것

## 7. UI구성
- **상단바**: 데이터 소스 토글([Notion]/[Sheets]), 필터(전체/책/영화),
  정렬(완료일순/평점순), "+ 새 기록 추가" 버튼
- **본문**: 반응형 카드 그리드 갤러리
- **카드**: 커버이미지(책=3:4 세로, 영화=16:9 가로, 없으면 placeholder),
  유형별 색상 뱃지, 별점(⭐), 한줄평, 완료일
- **모달**: 등록/수정 폼 (제목/유형/평점/한줄평/완료일/커버이미지 URL)

## 8. 성공지표
- Notion, Google Sheets 양쪽 백엔드에서 Read/Create/Update/Delete가
  로컬 환경에서 모두 정상 동작
- Vercel 배포 주소로 외부에서 정상 접속 가능
- 실제 책/영화 5건 이상을 등록하고 갤러리에서 정상 조회

## 9. 향후고려사항
- 커버이미지 자동 검색 연동 (책=Open Library, 영화=TMDB)
- 제목 검색 기능
- 유형별/월별 통계 대시보드
- 다중 사용자 지원 (로그인, 공유 갤러리)

## Open Questions (아직 정해지지 않은 것)
- 정렬 기본값을 완료일순/평점순 중 무엇으로 할지 미정
- 삭제 시 확인 창 문구와 되돌리기(undo) 제공 여부 미정
- Notion과 Sheets 중 앱을 처음 열었을 때 기본으로 보여줄 데이터 소스 미정
